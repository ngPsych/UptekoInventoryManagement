import React from 'react';
import { View, Text } from 'react-native';

export const AssemblyScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Assembly</Text>
        </View>
    );
};