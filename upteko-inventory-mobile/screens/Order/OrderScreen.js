import React from 'react';
import { View, Text } from 'react-native';

export const OrderScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Order</Text>
        </View>
    );
};