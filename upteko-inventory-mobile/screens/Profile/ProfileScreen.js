import React from 'react';
import { View, Text } from 'react-native';

export const ProfileScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};