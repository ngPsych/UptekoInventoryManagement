import React from 'react';
import { View, Text } from 'react-native';

export const InventoryScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Inventory</Text>
        </View>
    );
};
